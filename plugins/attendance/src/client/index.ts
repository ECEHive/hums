/**
 * Attendance plugin client manifest.
 *
 * Contributes admin pages for shift scheduling, attendance tracking, periods,
 * and reports. The full implementation will be fleshed out in Phase 11 when
 * admin UI components are migrated to the plugin.
 */
export const attendanceClientManifest = {
	pages: [],
} as const;
